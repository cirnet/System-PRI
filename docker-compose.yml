version: "3.8"
services:
  postgres:
    image: postgres:12.11-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=test_db
  django:
    build: .
    ports:
      - target: 8000
        published: 8000
    depends_on:
      - postgres
